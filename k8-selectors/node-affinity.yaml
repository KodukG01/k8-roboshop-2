---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-affinity
  labels:
    environment: dev
spec:
  selector:
    matchLabels:
      environment: dev
  template:
    metadata:
      labels:
        environment: dev
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: hardware
                operator: In 
                - gpu
      containers:
      - name: with-node-affinity
        image: nginx
        